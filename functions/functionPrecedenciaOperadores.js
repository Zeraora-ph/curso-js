/* Operator precedence
    Precedencia de operadores

 * grouping                     ( )     
 * negação                      ! ++ --
 * multiplicação e divisão      * /
 * adição e subtração           + -
 * relacional                   < <= > >=c
 * igualdade                    == != === !==
 * AND                          &&
 * OR                           ||
 * conditional                  ?:
 * assignment (atrubuição)      = += -= *=
*/

console.log( false == 0 );